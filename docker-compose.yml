services:

  backend:
    build:
      context: ./Backend
    container_name: onecart-backend
    env_file:
      - ./Backend/.env
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./Frontend
    container_name: onecart-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend